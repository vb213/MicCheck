<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.MicCheck" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary accent -->
        <item name="colorPrimary">@color/accent</item>
        <item name="colorSecondary">@color/accent</item>

        <!-- Background -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>

        <!-- Text -->
        <item name="android:textColorPrimary">@color/textPrimary</item>
        <item name="android:textColorSecondary">@color/textSecondary</item>

        <!-- Ripple / selection -->
        <item name="colorControlHighlight">@color/accentAlpha</item>

        <!-- Rounded components -->
        <item name="shapeAppearanceSmallComponent">@style/RoundedSmall</item>
        <item name="shapeAppearanceMediumComponent">@style/RoundedMedium</item>
        <item name="shapeAppearanceLargeComponent">@style/RoundedLarge</item>
    </style>

</resources>